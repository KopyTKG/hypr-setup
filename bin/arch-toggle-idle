#!/bin/bash
if pgrep -x hypridle >/dev/null; then
  pkill -x hypridle
  notify-send -u low "Idle lock: off"
else
  setsid uwsm-app -- hypridle >/dev/null 2>&1 & disown
  notify-send -u low "Idle lock: on"
fi
pkill -RTMIN+9 waybar 2>/dev/null
