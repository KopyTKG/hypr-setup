#!/bin/bash
# arch-power-profile-menu — pick a power profile via spot tile picker and apply it.

set -e

current=$(powerprofilesctl get 2>/dev/null || echo balanced)

choice=$(SPOT_DEFAULT="$current" spot ~/.config/spot/power-profile.toml) || exit 0

[[ -n $choice ]] && powerprofilesctl set "$choice"
