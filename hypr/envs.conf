# Extra env variables
# env = MY_GLOBAL_ENV,setting

# NOTE: env values containing `#` (hex colors, anchors, etc.) can't live here —
# Hyprland treats `#` as a comment. Use ~/.config/environment.d/ for those.
# (FZF_DEFAULT_OPTS with Tokyo Night palette lives in ~/.config/environment.d/fzf.conf)
