# Custom window rules

# Floating installer terminals (Pacman / AUR / Development via mise)
windowrule = float on, match:class arch-installer
windowrule = center on, match:class arch-installer
windowrule = size 1400 900, match:class arch-installer
