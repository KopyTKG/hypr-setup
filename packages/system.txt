# System-layer packages — read by bootstrap-system.sh.
# Everything here mirrors Omarchy's install/login + install/config defaults.
# These are services and daemons required for a working desktop, distinct
# from the user-app packages in pacman.txt / aur.txt.

# --- Greeter ---
sddm

# --- Wayland session orchestration ---
uwsm

# --- Networking (replaces networkmanager) ---
# systemd-networkd + systemd-resolved come with systemd; no package needed.
iwd
openresolv

# --- Bluetooth ---
bluez
bluez-utils

# --- Printing ---
cups
cups-browsed
cups-filters
cups-pdf
system-config-printer

# --- mDNS / service discovery ---
avahi
nss-mdns

# --- Keyring + polkit (Omarchy uses polkit-gnome, not hyprpolkitagent) ---
gnome-keyring
polkit-gnome

# --- XDG portals (in addition to xdg-desktop-portal-hyprland) ---
xdg-desktop-portal-gtk

# --- Firewall ---
ufw

# --- Power management ---
power-profiles-daemon

# --- Boot splash (theme set up separately) ---
plymouth

# --- BTRFS snapshots (init separately) ---
snapper
